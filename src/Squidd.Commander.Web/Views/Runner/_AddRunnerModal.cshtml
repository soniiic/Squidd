<div class="modal fade center" id="addRunnerModal" tabindex="-1" role="dialog" aria-labelledby="addRunnerModallLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="addRunnerModalLabel">Add New Runner</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <p>Ensure that you have the runner running and have the <a href="">pair passphrase</a> to hand.</p>
                <form>
                    <div class="form-group">
                        <label for="endpoint">IP Address</label>
                        <input type="email" class="form-control" id="endpoint" name="EndPoint" placeholder="12.34.56.78:13000">
                        <small id="emailHelp" class="form-text text-muted">If the port is not defined, it will default to 13000</small>
                    </div>
                    <div class="form-group">
                        <label for="pairpassphrase">Pair Passphrase</label>
                        <input type="text" class="form-control" id="pairpassphrase" name="PairPassphrase" aria-describedby="emailHelp" placeholder="A1B2C3" maxlength="6">
                    </div>
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                <button type="button" class="btn btn-primary">Pair</button>
            </div>
        </div>
    </div>
</div>